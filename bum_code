from pygame import *
from random import*
from time import time as timer
font.init()
mixer.init()

clock= time.Clock()
win_width=1000
win_height=700
back = (0,100,0)

game = True
finish = False

window=display.set_mode((win_width, win_height))
display.set_caption('пинг понг')
background= transform.scale(image.load('B2.png'),(win_width,win_height))

FPS =165
clock = time.Clock()

game = True
Finish = False

class GameSprite(sprite.Sprite):
    def __init__(self, q_image, x, y, speed, size_x, size_y):
        super().__init__()
        self.image = transform.scale(image.load(q_image), (size_x, size_y))
        self.speed = speed
        self.rect = self.image.get_rect()
        self.rect.x = x
        self.rect.y = y
        self.size_x = size_x
        self.size_y = size_y
    def reset(self):
        window.blit(self.image, (self.rect.x, self.rect.y))

class Player(GameSprite):
    def update_l(self):
        keys = key.get_pressed()
        if keys[K_w] and self.rect.y > 5:
            self.rect.y -= self.speed
        if keys[K_s] and self.rect.y < win_width - 40:
            self.rect.y += self.speed

    def update_r(self):
        keys = key.get_pressed()
        if keys[K_UP] and self.rect.y > 5:
            self.rect.y -= self.speed
        if keys[K_DOWN] and self.rect.y < win_width - 80:
            self.rect.y += self.speed

class ball(GameSprite):
    def update(self):
        if self.rect.y>=win_height:
            self.rect.x -=randint(50,650)

score_1 = 0
score_2 = 0

speed_x = 3
speed_y = 3

font = font.Font(None,80)
lose1 = font.render('Player1 lose!',True,(100, 0 ,0))
score1_t = font.render('score:'+str(score_1), True,(180,0,0))
score2_t = font.render(str(score_2),True,(180,0,0))


pong= Player('pong.png',10,300,10,80,120)
ping= Player('pong.png',920,300,10,80,120)
ball = ball('ball.png',300,150,3,20,80)


while game == True:
    for e in event.get():
        if e.type == QUIT:
            game = False
    if finish == False:
        window.blit(background,(0,0))
        window.blit(score1_t, (130,100))
        window.blit(score2_t, (730,100))
        pong.reset()
        pong.update_l()
        ping.reset()
        ping.update_r()
        ball.reset()
        ball.rect.x += speed_x
        ball.rect.y += speed_y
        if ball.rect.y>win_height - 50 or ball.rect.y<0:
            speed_y *= -1
        if sprite.collide_rect(pong,ball) or sprite.collide_rect(ping,ball):
            speed_x *=-1
        if ball.rect.x<0:
            score_2 +=1
        if ball.rect.x<920:
            score_1 +=1

    display.update()
    clock.tick(60)
    display.update()
    clock.tick(60)
